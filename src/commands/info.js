const {MessageEmbed} = require("discord.js");

module.exports = {
  name:"info",
  execute(message){
    var embed = new MessageEmbed();
    embed.setTitle("Info")
    .setDescription("BloxFlip is an open source dicord bot made by mmccall0813 on github. Its main feature is the coinflip, which uses ")
  }
}